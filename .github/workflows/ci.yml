name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    name: Test (node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [22]
    services:
      # 内建 MySQL service 容器，避免 Docker-in-Docker
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_dev_password
          MYSQL_DATABASE: dsh_storage
          MYSQL_USER: dsh
          MYSQL_PASSWORD: dsh_dev_password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot_dev_password"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      STORAGE_HOST: 127.0.0.1
      STORAGE_PORT: "3306"
      STORAGE_USER: dsh
      STORAGE_PASSWORD: dsh_dev_password
      STORAGE_DATABASE: dsh_storage
      STORAGE_TABLE_PREFIX: dsh_stor_
      STORAGE_TEST_DATABASE: test
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Typecheck
        run: pnpm typecheck
      - name: Lint
        run: pnpm lint
      - name: Build
        run: pnpm build
      - name: Test (unit)
        run: pnpm test:unit
      - name: Test (integration + e2e, with coverage gate)
        run: pnpm test:coverage
